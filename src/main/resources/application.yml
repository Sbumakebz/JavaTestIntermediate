spring:
  application:
    name: fluidity-JavaTestIntermediate

server:
  port: 8080

logging:
  level:
    root: INFO
    solutions.fluidity: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
  file:
    name: logs/fluidity-app.log

